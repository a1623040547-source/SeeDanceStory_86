# Seedance API key（勿提交）
.env
.env.local
*.env.local

# macOS
.DS_Store

# IDE
.idea/

# 上传脚本本地配置（含服务器路径/域名，勿提交）
scripts/upload-config.local

# 阶段测试与整段合成视频（本地预览用，不提交）
**/phase_test_*.mp4
**/*_merged.mp4

# 所有合成/生成的视频（phase 段、过渡、跨 section 合并等，不提交）
**/phases/**/*.mp4
**/section*_*_to_section*.mp4

# phase 视频均匀截帧（本地素材用，不提交）
**/frames/